# Infrastructure Text Formatting

`text_format.py` contains dependency-free CJK display-width and numeric-format
helpers used by infrastructure display and KIS event output. It replaces the
retired `utils.format_utils` module.
