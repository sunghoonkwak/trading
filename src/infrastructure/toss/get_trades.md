# Toss Trades Query (`src/infrastructure/toss/get_trades.py`)

`get_trades()` reads the authenticated, account-scoped Toss trade history using
the helper's supported date and pagination filters. It returns the API result
object and is read-only.
