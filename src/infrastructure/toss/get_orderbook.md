# Toss Orderbook Query (`src/infrastructure/toss/get_orderbook.py`)

`get_orderbook()` reads the Toss orderbook for a symbol through the shared
authenticated client and returns the validated result object.
