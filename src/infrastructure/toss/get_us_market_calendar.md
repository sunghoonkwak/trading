# Toss US Market Calendar Query (`src/infrastructure/toss/get_us_market_calendar.py`)

`get_us_market_calendar()` reads the Toss US market-calendar endpoint for a
requested date range and returns the API result object. It is a read-only
market-data helper.
