# Toss Sellable-Quantity Query (`src/infrastructure/toss/get_sellable_quantity.py`)

`get_sellable_quantity()` reads the account-scoped quantity currently available
to sell for a symbol. It returns the API result object and never places an
order.
