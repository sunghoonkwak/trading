# Toss Price-Limit Query (`src/infrastructure/toss/get_price_limit.py`)

`get_price_limit()` reads the Toss price-limit information for a symbol and
returns the validated result object through the common authenticated client.
