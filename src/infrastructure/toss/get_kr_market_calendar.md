# Toss Korean Market Calendar Query (`src/infrastructure/toss/get_kr_market_calendar.py`)

`get_kr_market_calendar()` reads the Toss Korean market-calendar endpoint for
a requested date range and returns the API result object. It is a read-only
market-data helper.
