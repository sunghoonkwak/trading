# Toss Exchange-Rate Query (`src/infrastructure/toss/get_exchange_rate.py`)

`get_exchange_rate()` reads the Toss exchange-rate endpoint for the requested
currency pair through the common authenticated client and returns its validated
result object.
